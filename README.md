# eks-rbac-audit
Audit tool for detecting risky RBAC permissions in EKS clusters

`eks-rbac-audit` is a Python-based CLI tool that analyzes Kubernetes RBAC roles and bindings in AWS EKS for potential privilege escalation, insecure defaults, and orphaned permissions.
